# 任务：统一小说工作台布局与导航

## 1. 一期：绑定会话入口收敛

- [x] 1.1 实现统一的绑定会话打开动作（按绑定关系路由：绑定会话进工作台写作视图，未绑定会话进独立会话视图），验证：单元测试覆盖绑定/未绑定两种路由结果（packages/app）
- [x] 1.2 收敛 sessions 页最近列表入口到统一打开动作，验证：点击最近列表中的绑定会话落在工作台写作视图
- [x] 1.3 收敛小说分组侧栏入口到统一打开动作，验证：点击侧栏中的绑定会话落在工作台写作视图
- [x] 1.4 收敛会话内小说面板入口到统一打开动作，验证：从小说面板打开绑定会话落在工作台写作视图
- [x] 1.5 收敛命令面板入口到统一打开动作，验证：从命令面板选择绑定会话落在工作台写作视图
- [x] 1.6 验证绑定会话不注册会话标签，验证：经任一入口打开绑定会话后，顶部会话标签栏无新增标签

## 2. 一期：三区布局与对话常驻

- [x] 2.1 重构工作台为三区结构，使左栏三分段（章节/大纲/设定）常驻可用，验证：切换主区视图与左栏分段时左栏保持可见、其余两区不重置
- [x] 2.2 实现右栏图标列与面板互斥切换（对话/人物/伏笔/张力/结构/批注），验证：点击图标切换面板且原面板状态保留
- [x] 2.3 实现对话面板默认激活与常驻保持，验证：进入工作台右栏为对话，主区往返后对话内容与滚动位置不变
- [x] 2.4 移除主区全宽分支与人物双入口，画布保留为主区视图，验证：人物仅通过右栏图标列一处进入，关系/地图/画布行为不变
- [x] 2.5 一期收尾：在 packages/app 通过 `bun typecheck` 与 oxlint，并手动走查一期全部场景

## 3. 二期：面板双态与联动

- [x] 3.1 实现面板 peek/expand 双态与默认深度（结构 expand，对话/人物/伏笔/张力/批注 peek），验证：首次点结构面板占主区、首次点伏笔面板在右栏
- [x] 3.2 实现 expand 态自动收边与对话窄条（生成中显示活动指示，点击恢复对话），验证：进入展开态左右栏收起、窄条指示与恢复可用
- [x] 3.3 实现 Esc/返回从 expand 回 peek 并恢复之前的右栏面板，验证：展开态按 Esc 后右栏恢复展开前的活动面板
- [x] 3.4 实现 Inspector 联动（人物/伏笔/批注跟随阅读视图章节选中），验证：切换章节后各面板聚焦内容随之更新

## 4. 二期：持久化与自动收边

- [x] 4.1 实现布局状态持久化（带版本号的持久化键承载右栏面板/展开态/左栏收起态），验证：修改布局后重启应用恢复所选状态
- [x] 4.2 实现历史格式布局数据的安全降级，验证：构造无法识别的旧数据后应用按默认值运行且不报错
- [x] 4.3 实现窄宽度自动收左栏与手动操作优先规则，验证：缩窄窗口左栏自动收起，手动展开后保持展开直至宽度回到阈值以上
- [x] 4.5 实现右栏手动收起与展开（工具栏按钮 + 持久化，退出展开态恢复显示），验证：收起后主区获得完整宽度，重启后恢复收起态，展开态返回后右栏重新可见
- [x] 4.6 实现右栏第二档自动收边（可用宽 <1100 自动收右栏，手动优先），验证：窗口在 1008~1100 区间右栏自动收起且左栏保持，低于 1008 两档依次收起
- [x] 4.4 二期收尾：在 packages/app 通过 `bun typecheck` 与 oxlint，回归走查规格全部场景（含旧路由深链 `/novel/:novelID/session/:id` 直接访问）
