# 任务：工作台分栏宽度可拖拽调节

## 1. 宽度状态与阈值派生

- [x] 1.1 扩展布局持久化结构（leftWidth/railWidth 可空字段，宽容读取回落默认），验证：构造含/不含宽度字段的持久化数据，应用分别按设定值与默认值运行且不报错
- [x] 1.2 自动收边阈值改为从设定宽度派生（左档 = 左栏宽 + 720；右档 = max(右栏宽 + 720, 左栏宽 + 721)，阈值派生提取纯函数），验证：单测覆盖默认值等价（1100/1008）与右栏窄于左栏时右档 > 左档的顺序保证；走查拖宽右栏后自动收起点随之右移

## 2. 拖拽交互

- [x] 2.1 为左右栏接入手柄（复用 packages/ui ResizeHandle），拖拽实时更新内存态、松手提交持久化，验证：拖动右栏手柄宽度实时变化，拖动中无卡顿，重启后恢复设定宽度
- [x] 2.2 实现双击手柄重置默认宽度，验证：双击已调宽的右栏手柄后恢复 380px 并持久化
- [x] 2.3 实现拖到最小宽度收起联动（collapseThreshold → 手动收起态，收起时设定宽度提交为最小值），验证：右栏拖过 320px 后继续收窄方向拖动即收起，再展开宽度为 320px，窗口拉宽后回归自动规则

## 3. 走查收尾

- [x] 3.1 在 packages/app 通过 `bun typecheck` 与 oxlint，跑通 bun test
- [x] 3.2 回归走查：拖拽/双击/拖到头收起/重启恢复宽度、窄窗口手动展开右栏后主区可用性（阅读纸面自适应）、左栏 220/288/400 三档宽度下章节列表可用性、右栏 320px 下对话输入区可用性
