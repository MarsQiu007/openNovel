# 任务：标题栏书籍标签页（tab 体系重定义）

## 1. 一期：书籍标签上线（与既有会话标签共存）

- [x] 1.1 Tab 联合类型追加书籍标签（server/dir/novelID 显式字段），派生路由与唯一键，持久化迁移宽容旧数据，验证：单测覆盖 tabHref/tabKey 派生与旧数据原样保留
- [x] 1.2 布局层路由解析将工作台路由升为一等类型（无会话与书内会话路由同属一个书籍路由），排除建书向导路由（`/:dir/novel/wizard` 不产出 novel 路由），验证：单测覆盖两种路由形态解析出同一书籍路由、向导路由不产出书籍路由、不再降格为首页
- [x] 1.3 标题栏注册副作用追加书籍分支，活跃标签按书籍路由匹配，最近标签记忆对书籍路由生效，验证：打开书籍出现活跃标签，离开工作台回书架后标签常驻且记忆保留
- [x] 1.4 书籍标签槽渲染：异步取书名 + 持久化缓存回写 + 重启立即显示 + 书名更新跟随，验证：单测覆盖书名缓存回写；走查首开占位、重启秒显
- [x] 1.5 标签关闭与重开接入最近关闭栈（关闭活跃标签后切相邻标签、重开回工作台），验证：单测覆盖书籍标签的入栈与重开；走查关闭回书架、重开进工作台、数据无损
- [x] 1.6 一期校验：在 packages/app 通过 `bun typecheck` 与 oxlint，跑通 bun test
- [x] 1.7 一期走查：打开书注册/书间一键切换/标签常驻/同书同标签/mod+1..9 切书/拖拽排序/重启恢复标签与书名、与会话标签混排期无布局异常

## 2. 二期：会话标签退役（到达终态）

- [x] 2.1 Tab 联合类型收缩为书籍标签，持久化迁移一次性清除遗留会话/草稿标签，最近标签/标题缓存/最近关闭栈的孤儿键同步清理，验证：单测覆盖旧数据清洗（含 mixed 数组、孤儿键）且不报错
- [x] 2.2 标签栏的会话/草稿槽与绑定会话标签清洗逻辑退役，遗留会话路由清理，验证：grep 无会话标签注册路径残留，typecheck 通过
- [x] 2.3 草稿流程页面态化：草稿 prompt 状态迁为页面级，提交首条消息后直接导航会话路由（无标签注册），验证：新建会话全程无标签出现，提交后落在会话视图
- [x] 2.4 命令面板标签命令语义跟随（切书/关书籍标签）与 i18n 文案更新，验证：命令面板执行切书与关标签正确，文案无"会话标签"残留
- [x] 2.5 二期校验：在 packages/app 通过 `bun typecheck` 与 oxlint，跑通 bun test
- [x] 2.6 二期走查：自由会话从会话页面进出无标签、草稿无标签、构造含遗留会话标签的持久化数据启动后自动清除且无错误、多书标签全流程复测（走查反馈的 + 按钮删除与会话页新建死锁已修复；切书状态重置另立 change `novel-workspace-per-book-state`）
