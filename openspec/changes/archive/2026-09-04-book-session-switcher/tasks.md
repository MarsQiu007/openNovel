# 任务：书内会话切换器

## 1. 查询与数据

- [x] 1.1 书内会话列表查询组合：绑定关系 × 会话列表取交集并过滤已归档，接入查询缓存（新建/归档后失效重取），验证：单测覆盖交集与归档剔除、绑定变更后列表刷新

## 2. 切换器 UI

- [x] 2.1 对话面板顶部切换器：显示当前会话标题、下拉列出书内会话并标识当前项、选择即更新工作台路由会话段，验证：切换后主区焦点视图与左栏分段不变，A→B→A 往返对话内容保持不变
- [x] 2.2 新建轻会话：创建会话（标题用书名）+ 绑定当前书籍 + 切入新会话，不发送任何自动 prompt，验证：新建后对话面板切入空会话，绑定关系建立，后端无 prompt 调用，切换器列表即时出现新会话
- [x] 2.3 空态与过滤边界：无绑定会话时对话面板保持现有空态引导（不渲染切换器），归档会话不出现在下拉，验证：走查新书与含归档会话的书籍两种形态

## 3. 走查收尾

- [x] 3.1 在 packages/app 通过 `bun typecheck` 与 oxlint，跑通 bun test
- [x] 3.2 回归走查：生成任务进行中切换会话再切回（原会话后台生成、进度可见）、面板 expand 态下切换器随面板隐藏、Esc 返回后恢复、多会话书籍全流程（切换/新建/生成）
