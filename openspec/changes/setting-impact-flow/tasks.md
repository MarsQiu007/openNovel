## 1. 影响面类型与服务骨架

- [x] 1.1 在 `packages/plugin` 新增影响面类型定义，覆盖变更意图、影响发现、影响任务和影响报告，并用 `bun typecheck` 验证类型通过
- [x] 1.2 新增 `SettingImpactService` 骨架，提供影响计划查询、任务生成和报告汇总入口，并用单元测试验证空变更返回明确错误
- [x] 1.3 为服务设计稳定任务键，并用单元测试验证同一变更和同一受影响对象不会重复生成任务

## 2. 确定性影响识别

- [x] 2.1 实现基于 `entity_refs` 的确定性影响扫描，覆盖章节正文、章节摘要、卷摘要和设定描述引用，并用母子角色改名夹具验证任务包含旧值、新值和证据
- [x] 2.2 实现角色、关系、伏笔和剧情线的结构性依赖扫描，并用单元测试验证关系端点、伏笔章节和剧情线变化能生成任务
- [x] 2.3 为世界观标题和关键内容变化实现引用重建与任务生成，并用单元测试验证重复应用不会产生重复任务

## 3. 语义影响建议

- [x] 3.1 定义语义影响建议的数据结构与确认状态，并用单元测试验证未确认建议不会进入执行任务
- [x] 3.2 提供把语义建议转为正式任务或标记忽略的能力，并用单元测试覆盖确认、拒绝和重复提交三种场景
- [x] 3.3 更新 director 提示词，要求语义影响必须等待用户确认，并用提示词快照测试验证规则存在

## 4. 工具与修改入口接入

- [x] 4.1 新增影响计划查询工具，保证只读模式不修改数据库，并用工具级测试验证返回影响计划和证据
- [x] 4.2 将 `manage_characters` 接入统一服务，并用测试验证角色更新后返回影响摘要且生成任务
- [x] 4.3 将 `update_setting` 接入统一服务，覆盖世界观、剧情线、伏笔和关系更新，并用测试验证任务生成
- [x] 4.4 将 `save_novel_settings`、`create_relationship`、`organize_settings` 接入统一服务或复用其任务生成逻辑，并用测试验证不绕过影响任务
- [x] 4.5 将候选区接受、候选区合并和 observer 状态提交接入统一服务，并用测试验证正式表变化会生成影响任务

## 5. 报告、提示词与门禁

- [x] 5.1 统一工具输出的影响报告字段，包含新增任务数、待处理数、高优先级摘要和建议动作，并用快照测试验证输出
- [x] 5.2 更新 director、architect、observer 和 pipeline 提示词，要求修改设定后查看并处理影响任务，并用提示词测试验证关键规则存在
- [x] 5.3 扩展写作门禁，使存在未完成影响任务时阻止 `write_chapter` 和 `revise_chapter`，并用集成测试验证门禁提示包含待处理数量
- [x] 5.4 实现影响任务批量处理与单独处理入口，并用测试验证成功、失败、跳过和残留数量报告正确

## 6. 数据兼容与迁移

- [x] 6.1 如需扩展任务元数据，在 `packages/novel-store` 做兼容性增量迁移，并用迁移测试验证旧数据可读
- [x] 6.2 用包含旧任务和旧引用的数据库夹具验证新工具可以查看、完成或跳过历史任务
- [x] 6.3 验证全量引用重建不会破坏既有章节、角色、卷和设定数据

## 7. 质量验证

- [x] 7.1 在 `packages/plugin` 运行 `bun typecheck`，确认无类型错误
- [x] 7.2 在 `packages/plugin` 运行 `bun test --timeout 30000 --only-failures`，确认相关测试全部通过
- [x] 7.3 在仓库根运行 oxlint 检查新增和修改文件，确认无 lint 错误
- [x] 7.4 在仓库根运行 `bun run typecheck`，确认全仓类型检查通过
- [x] 7.5 运行 `openspec validate setting-impact-flow`，确认提案与规格通过校验
