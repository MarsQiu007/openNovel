## Tasks

### 生成流程

- [ ] 生成提示词：画布几何约定、世界观条目注入、指令注入、结构化 JSON 输出要求。
- [ ] 生成服务端流程：调用 LLM、Effect Schema 解码校验、失败整体拒绝。
- [ ] 校验失败自动重试：错误反馈注入提示词重试一次，仍失败转手动。
- [ ] 事务性草稿写入：清除旧草稿内容后写入新要素。
- [ ] 生成会话工具/端点接入，进度状态上报。

### 界面

- [ ] 地图页 AI 生成入口：条目范围选择、可选指令输入。
- [ ] 已有草稿时的覆盖确认（列出将被覆盖的要素数）。
- [ ] 生成进度展示（生成期间锁定入口，无取消）、失败提示与重试；完成后进入草稿视图。

### 验证

- [ ] 生成流程测试：校验通过写入、校验失败不残留、替换语义、正式地图不受影响。
- [ ] packages typecheck 通过。
- [ ] openspec validate map-ai-draft --strict 通过。
