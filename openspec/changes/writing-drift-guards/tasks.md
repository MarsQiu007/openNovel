## 1. 角色绑定视图

- [ ] 1.1 在 packages/plugin 中构建命名角色绑定视图，并用单元测试验证正式角色名、活跃状态和章纲相关性能进入视图
- [ ] 1.2 接入 `relationship-hard-context` 的方向化关系与亲属称谓绑定，并用单元测试验证唯一称谓绑定、歧义称谓和未解析标记的传递结果

## 2. Writer 生成防护

- [ ] 2.1 将命名角色白名单、亲属称谓绑定和新增角色例外写入 writer 上下文与提示词，并用快照测试验证母子场景输出
- [ ] 2.2 在章节提交前实现保守的亲属称谓实体化检查，并用单元测试覆盖唯一绑定、未绑定实体化和普通对话称谓三种文本
- [ ] 2.3 让无法唯一解析的实体化称谓生成包含原文证据的审计提示，并用单元测试验证提示内容

## 3. Auditor 关系审计

- [ ] 3.1 更新 auditor 前置动作，强制读取角色关系权威数据，并用提示词测试验证必读关系步骤存在
- [ ] 3.2 更新 pipeline 审计调度，把相关关系和角色绑定视图传给 auditor，并用单元测试验证轻量设定审计显式覆盖关系连续性维度
- [ ] 3.3 增加正文引入错误儿子的审计样例，验证关系连续性维度能输出 FAIL 并引用正文与关系设定证据

## 4. Observer 与状态提交防护

- [ ] 4.1 在状态提交处理角色或关系前复用角色绑定视图解析本地 ID、姓名和亲属称谓，并用单元测试覆盖 UUID、唯一姓名和唯一称谓
- [ ] 4.2 将无法唯一解析的称谓型角色送入候选区，并用单元测试验证不会写入正式角色表
- [ ] 4.3 在新强关系与既有强关系冲突时生成带冲突原因的候选关系，并用单元测试验证既有母子关系不会被自动覆盖
- [ ] 4.4 更新 observer 提示词，要求亲属称谓先绑定既有角色，无唯一解析时进入候选区，并用提示词快照测试验证规则存在

## 5. 质量验证

- [ ] 5.1 在 packages/plugin 目录运行 `bun typecheck`，确认无类型错误
- [ ] 5.2 在 packages/plugin 目录运行 oxlint，确认无 lint 错误
- [ ] 5.3 用母子关系测试夹具覆盖 writer 守卫、auditor 输入、observer 解析和状态提交解析，验证四个阶段使用同一角色绑定结果
