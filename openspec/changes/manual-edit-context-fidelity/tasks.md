## 1. 编辑目录与契约

- [x] 1.1 在共享 schema 中定义手动编辑类别、同步状态、错误载荷和同步状态查询契约，并用 typecheck 验证类型导出
- [x] 1.2 建立服务端手动编辑目录，覆盖现有小说 UI 写入端点的实体、字段、类别、AI 感知策略和同步策略，并用单元测试断言所有正式数据端点已登记
- [x] 1.3 为书籍元数据与风格指南定义联合保存端点，并为手动编辑同步状态定义查询端点；在 packages/client 运行 `bun run generate` 验证 SDK 更新

## 2. 数据层与迁移

- [x] 2.1 在 packages/novel-store 新增手动编辑同步队列表和状态字段，支持已同步、待同步、同步失败、已跳过与来源指纹，并用迁移测试验证旧数据库可打开
- [x] 2.2 为章节摘要、实体引用、段摘要和结构化故事主轴增加来源指纹或等价新鲜度元数据，并标记受正文变化影响的下游条目，并用测试验证缺失历史指纹会标记为待校验
- [x] 2.3 实现同步状态读取、按来源指纹去重入队和同章节合并方法，并用单元测试覆盖重复保存、连续编辑和失败重试
- [x] 2.4 保留 `novels.story_spine` 兼容渲染缓存，同时新增结构化主轴条目存储，并用迁移测试验证旧主轴文本转为 legacy 或待校验条目
- [x] 2.5 实现历史手动编辑中间状态扫描与可读同步状态标记，并用迁移测试验证旧版多步保存留下的数据不会显示为完全已保存

## 3. 服务端保存一致性

- [x] 3.1 引入服务端手动编辑事务辅助层，统一校验目标归属、执行正式表写入、创建同步记录并返回显式成功或错误，用测试覆盖目标不存在时无字段更新
- [x] 3.2 将章节正文保存、版本恢复、章纲保存和正式设定编辑接入统一事务辅助层，并用 HTTP API 测试验证同步记录与保存结果一致
- [x] 3.3 实现书籍元数据与风格指南联合保存的事务端点，用测试验证任一部分失败时两部分都不落库
- [x] 3.4 消除章纲、卷纲等端点的静默跳过路径，返回明确目标不存在错误，并用 API 测试覆盖错误契约

## 4. AI 同步执行

- [x] 4.1 在 packages/opennovel 启动手动编辑同步 worker，消费队列并按目录调用确定性或 observer 同步流程，用集成测试验证 UI 保存后任务被处理
- [x] 4.2 将手动设定变更接入统一设定影响面，执行引用扫描、影响任务生成和稳定键去重，并用测试覆盖角色与世界观 UI 更新
- [x] 4.3 实现手动正文修订的 observer 重建流程，更新章节摘要、实体引用、受影响段摘要和受影响主轴条目，并用测试验证指纹从待同步变为已同步
- [x] 4.4 重建受影响章节的结构化故事主轴条目并刷新兼容缓存，用测试验证旧正文派生条目不会残留为最新事实
- [x] 4.5 为同步失败、重试和用户跳过实现可追溯状态转换，用测试验证失败原因保留且重复任务不会堆积

## 5. 上下文消费门禁

- [x] 5.1 扩展 writer 快照组装，检测章节摘要、实体引用、段摘要和故事主轴的新鲜度，并用测试验证过期数据被标记或排除
- [x] 5.2 在写作、修订和继续写作入口接入手动正文同步门禁，用测试验证未完成同步会返回重试或跳过提示且不写入新正文
- [x] 5.3 实现用户显式跳过后的写作放行与可追溯提示，用测试验证跳过状态可查询且不会重复阻塞同一编辑
- [x] 5.4 扩展 AI 设定影响门禁，使未处理的 UI 设定联动任务遵循既有影响面写作门禁，并用测试验证任务提示包含目标对象

## 6. 界面保存与状态反馈

- [x] 6.1 新增统一的编辑保存状态模型和 UI 反馈组件，展示保存中、已保存、保存失败、待同步、同步失败与已跳过，并用组件测试断言状态切换
- [x] 6.2 改造章节编辑器自动保存，失败时保留内容、显示失败状态并提供重试，用测试验证不会显示虚假的自动保存成功时间
- [x] 6.3 改造书籍信息与风格指南表单调用联合保存端点，并展示事务失败后的用户输入和错误，用测试验证不再串行触发两个独立保存
- [x] 6.4 在工作台展示待同步和同步失败列表，提供重试与显式跳过操作，并用测试验证操作后状态刷新
- [x] 6.5 审查新增界面状态文案来自既有文案或组件内简体中文常量，不修改 `packages/app/src/i18n/*.ts`，并用组件测试验证文案可见

## 7. 集成验收

- [x] 7.1 编写端到端场景：手动修改已摘要章节正文后，AI 写作先等待、重建或显式跳过，最终快照使用新指纹派生数据
- [x] 7.2 编写端到端场景：UI 修改角色名或世界观关键内容后，引用章节生成影响任务且去重，AI 写作能查看任务状态
- [x] 7.3 运行 packages/novel-store、packages/plugin、packages/server、packages/opennovel、packages/app 的 `bun test` 与 `bun typecheck`
- [x] 7.4 运行 `bun run generate`、全仓库 `bun run typecheck` 和 `bun run lint`，确认退出码均为 0
- [x] 7.5 运行 packages/opennovel 的 HttpApi exercise，确认新增端点、错误契约、SDK 调用和同步状态流程全部覆盖
- [x] 7.6 使用旧数据库 fixture 验证阅读、导出、基础编辑和新增同步状态标记共存，不因缺少历史指纹或目录元数据失败

