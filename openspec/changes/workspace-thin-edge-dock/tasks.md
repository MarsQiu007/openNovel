## 1. 宽度与状态基础

- [x] 1.1 移除薄边坞宽度常量，让 `collapseThresholds` 回到纯内容预留语义；在 `packages/app` 更新并运行阈值单测验证默认宽度、右栏拖窄、右栏拖宽和非法值场景。
- [x] 1.2 让左右分栏容器在收起态恢复 `hidden`，同时保持现有 `leftManual`、`railManual`、分栏宽度和右栏活动面板状态不变；通过组件状态走查确认展开/收起往返不重置导航和面板状态。

## 2. 展开态边缘收纳

- [x] 2.1 在左栏右缘和右栏左缘实现 hover/focus 淡入的虚化收纳条，点击分别收起对应分栏；移除页头和分栏头部的旧收纳入口。
- [x] 2.2 为展开态收纳条提供 tooltip、`aria-label` 和 `aria-expanded`，并确保键盘 focus 时控件可见；通过键盘 Tab 和读屏属性检查确认入口可发现且状态可理解。

## 3. 收起态边缘热区

- [x] 3.1 在 body 左右边缘实现极窄 overlay 热区：常显极淡线索，hover/focus 时淡入展开控件；点击恢复对应面板，且主区不因热区被挤压。
- [x] 3.2 拖拽调宽期间隐藏可点击收纳控件，右栏 expand 态复用右边缘入口返回常规布局；通过交互检查确认状态恢复且不与 resize 冲突。

## 4. 审查与验证

- [x] 4.1 检查现有布局持久化数据在升级和回滚场景下均可读取，非法或旧值按默认值/新收起形态安全处理；通过重启应用验证有效收起布尔值渲染为隐藏分区，非法或旧格式数据按默认值安全处理。
- [x] 4.2 在 `packages/app` 运行 `bun typecheck` 和相关单测，并在仓库根目录运行 `bun run lint`，确认无类型、lint 或回归错误。
- [x] 4.3 按规格逐条执行手动验收：展开态悬停收纳、左/右收起、边缘恢复、expand 恢复、窄屏自动收边、重启持久化和历史数据降级，确认界面无遮挡、焦点丢失或状态闪烁。
