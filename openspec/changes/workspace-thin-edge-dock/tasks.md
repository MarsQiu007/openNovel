## 1. 宽度与状态基础

- [x] 1.1 为左坞/右坞添加 36px 常量，并把保留宽度纳入 `collapseThresholds`；在 `packages/app` 更新并运行阈值单测验证默认宽度、右栏拖窄、右栏拖宽和非法值场景。
- [x] 1.2 让左右分栏容器在收起态切换为薄边坞宽度而不是 `hidden`，并保持现有 `leftManual`、`railManual`、分栏宽度和右栏活动面板状态不变；通过组件状态走查确认展开/收起往返不重置导航和面板状态。

## 2. 左侧导航薄边坞

- [x] 2.1 实现左坞设定、大纲、章节三个紧凑入口，当前模式高亮，点击仅切换主区焦点；通过交互检查确认左栏保持收起且左坞高亮更新。
- [x] 2.2 为左坞添加明确的展开箭头、tooltip、`aria-label` 和 `aria-expanded`，并移除页头左侧收纳箭头；通过键盘 Tab 和读屏属性检查确认入口可发现且状态可理解。

## 3. 右侧随行薄边坞

- [x] 3.1 实现右坞当前活动面板图标、展开入口和生成中活动指示；通过收起右栏后的界面检查确认活动面板可识别且面板状态保留。
- [x] 3.2 让右坞在 expand 态保持可见并提供返回常规布局入口，同时移除页头右侧收纳箭头；通过 peek/expand 往返检查确认活动面板恢复且手动收起被清除。
- [x] 3.3 保持右侧完整面板内容、拖拽手柄和图标列仅在展开态渲染，并保证窄宽度自动收边后主区内容宽度不低于 720px；通过缩放窗口和阈值单测验证两档收边顺序。

## 4. 审查与验证

- [x] 4.1 检查现有布局持久化数据在升级和回滚场景下均可读取，非法或旧值按默认值/薄边坞收起态安全处理；通过重启应用验证有效收起布尔值渲染为薄边坞，非法或旧格式数据按默认值安全处理。
- [x] 4.2 在 `packages/app` 运行 `bun typecheck` 和相关单测，并在仓库根目录运行 `bun run lint`，确认无类型、 lint 或回归错误。
- [x] 4.3 按规格逐条执行手动验收：左/右收起、左坞模式切换、expand 恢复、窄屏自动收边、重启持久化和历史数据降级，确认界面无遮挡、焦点丢失或状态闪烁。
