所有脚本通过 `bun script/<name>.ts` 直接执行；Windows 签名需 `GITHUB_ACTIONS=true` 且配置 `AZURE_TRUSTED_SIGNING_*` 环境变量；翻译脚本需安装 `opencode` CLI 并通过 `--model provider/id` 指定模型；发布脚本依赖 `@opencode-ai/script` 提供 `Script.version`/`Script.release`/`Script.preview`/`Script.channel`。