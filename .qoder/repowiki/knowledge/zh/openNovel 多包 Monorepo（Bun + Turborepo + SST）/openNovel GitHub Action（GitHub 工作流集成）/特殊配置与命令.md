本地开发通过 `bun index.ts` 运行，需设置 MODEL、ANTHROPIC_API_KEY、GITHUB_RUN_ID、MOCK_TOKEN、MOCK_EVENT 等环境变量模拟 GitHub Actions 环境；Action 安装通过 `opennovel github install` 交互式完成，或在 workflow 中直接使用 `MarsQiu007/openNovel/github@latest`。