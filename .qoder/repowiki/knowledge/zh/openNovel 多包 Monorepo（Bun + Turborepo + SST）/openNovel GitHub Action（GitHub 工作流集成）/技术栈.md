TypeScript + Bun（ESNext 模块、bundler 模式、noEmit 编译）、@actions/core 与 @actions/github 用于 GitHub Action 集成、@octokit/rest 与 @octokit/graphql 访问 GitHub REST/GraphQL API、@opencode-ai/sdk 与本地 opencode server（spawn 进程）通信、bun `$` shell 工具执行 git 命令。