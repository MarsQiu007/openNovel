部署通过 SST CLI 执行，不同 stage（production/dev/<自定义>）自动切换域名与资源；Console 本地开发使用 `bun db studio` 启动数据库管理；Lake Ingest 服务使用 `packages/stats/server/Dockerfile` 构建镜像并在 ECS Fargate 上运行；Secret 需预先在 SST secrets 中配置（如 GITHUB_APP_ID、EMAILOCTOPUS_API_KEY、STRIPE_SECRET_KEY 等）。