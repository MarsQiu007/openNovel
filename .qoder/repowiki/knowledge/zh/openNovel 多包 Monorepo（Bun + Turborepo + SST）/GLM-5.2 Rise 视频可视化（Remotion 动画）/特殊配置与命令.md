通过 `package.json` 中的 npm scripts 生成视频：`npm run render`（GLM-5.2）、`render:sheep`、`render:novel`、`render:flash`、`render:minimax` 分别输出到 `out/`；`still:june` 用于生成静态 PNG。运行前需安装依赖（Bun lock 文件存在，可用 `bun install` 或 `npm install`）。