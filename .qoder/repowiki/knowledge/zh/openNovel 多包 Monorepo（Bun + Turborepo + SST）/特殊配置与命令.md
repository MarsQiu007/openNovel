根命令：`bun dev` 启动 OpenCode CLI、`bun dev:desktop` 启动 Electron、`bun dev:web` 启动 Solid Web、`bun dev:all` 并行启动全部服务、`bun typecheck` 通过 Turborepo 执行全仓类型检查、`bun sso` 登录 AWS SSO、`bun translate:app` 调用翻译脚本。部署使用 `bun sst deploy --stage=<env>`，IaC 入口为 `sst.config.ts`。