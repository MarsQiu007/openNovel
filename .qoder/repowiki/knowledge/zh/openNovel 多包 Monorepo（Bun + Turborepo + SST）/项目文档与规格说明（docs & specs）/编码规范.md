- 规格文档采用「目标 → 现状 → 迁移步骤 → 验收标准」的结构化写法，每个章节末尾附带 checkpoint commit 消息模板和验证门禁。
- 配置类规格（如 `config.md`）使用表格逐项列出字段名、当前用途、状态标签（pending/keep/remove/redesign）及备注，便于逐组评审。
- 架构图与流程通过 ASCII 流程图、SQL DDL 片段、JSONC 示例在文档内自包含表达，不依赖外部图表工具。
- 文档标题统一使用中文，但保留英文技术标识（如 entity_refs、pending_updates、Effect、Drizzle、OpenTUI）以保持与源码一致。