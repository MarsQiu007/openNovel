---
title: CLI
description: OpenNovel CLI-alternativer og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

OpenNovel CLI starter som standard [TUI](/docs/tui) når den kjøres uten noen argumenter.

```bash
opennovel
```

Men den godtar også kommandoer som dokumentert på denne siden. Dette lar deg samhandle med OpenNovel programmatisk.

```bash
opennovel run "Explain how closures work in JavaScript"
```

---

### tui

Start OpenNovel-terminalbrukergrensesnittet.

```bash
opennovel [project]
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                              |
| ---------------------------------------- | ---- | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                   |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |      | Ledetekst som skal brukes                                                |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | Modell å bruke i form av leverandør/modell                               |
| <nobr><code>{"--agent"}</code></nobr>    |      | Agent som skal brukes                                                    |
| <nobr><code>{"--port"}</code></nobr>     |      | Port å lytte på                                                          |
| <nobr><code>{"--hostname"}</code></nobr> |      | Vertsnavn å lytte på                                                     |

---

## Kommandoer

OpenNovel CLI har også følgende kommandoer.

---

### agent

Administrer agenter for OpenNovel.

```bash
opennovel agent [command]
```

---

### attach

Koble en terminal til en allerede kjørende OpenNovel-backend-server startet via `serve` eller `web` kommandoer.

```bash
opennovel attach [url]
```

Dette gjør det mulig å bruke TUI med en ekstern OpenNovel-backend. For eksempel:

```bash
# Start the backend server for web/mobile access
opennovel web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
opennovel attach http://10.20.30.40:4096
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                                                          |
| ---------------------------------------- | ---- | ---------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |      | Arbeidskatalog for å starte TUI i                                                                    |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                                               |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`)                             |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Passord for grunnleggende autentisering (standard: `OPENNOVEL_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Brukernavn for grunnleggende autentisering (standard: `OPENNOVEL_SERVER_USERNAME` eller `opennovel`) |

---

#### create

Opprett en ny agent med tilpasset konfigurasjon.

```bash
opennovel agent create
```

Denne kommandoen vil veilede deg gjennom å lage en ny agent med en tilpasset systemmelding og verktøykonfigurasjon.

---

#### list

List opp alle tilgjengelige agenter.

```bash
opennovel agent list
```

---

### auth

Kommando for administrasjon av påloggingsinformasjon og innlogging for leverandører.

```bash
opennovel auth [command]
```

---

#### login

OpenNovel drives av leverandørlisten på [Models.dev](https://models.dev), så du kan bruke `opennovel auth login` til å konfigurere API-nøkler for enhver leverandør du vil bruke. Dette er lagret i `~/.local/share/opennovel/auth.json`.

```bash
opennovel auth login
```

Når OpenNovel starter opp, laster den leverandørene fra filen med påloggingsinformasjon. Og hvis det er noen nøkler definert i miljøene dine eller en `.env`-fil i prosjektet ditt.

---

#### list

Viser alle de autentiserte leverandørene som er lagret i filen med påloggingsinformasjon.

```bash
opennovel auth list
```

Eller bruk kortversjonen.

```bash
opennovel auth ls
```

---

#### logout

Logger deg ut av en leverandør ved å fjerne den fra filen med påloggingsinformasjon.

```bash
opennovel auth logout
```

---

### github

Administrer GitHub-agenten for depotautomatisering.

```bash
opennovel github [command]
```

---

#### install

Installer GitHub-agenten i depotet ditt.

```bash
opennovel github install
```

Dette setter opp den nødvendige GitHub Actions-arbeidsflyten og veileder deg gjennom konfigurasjonsprosessen. [Finn ut mer](/docs/github).

---

#### run

Kjør GitHub-agenten. Dette brukes vanligvis i GitHub Actions.

```bash
opennovel github run
```

##### Flagg

| Flagg                                 | Beskrivelse                                  |
| ------------------------------------- | -------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock-hendelse agenten skal kjøres for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personlig tilgangsnøkkel              |

---

### mcp

Administrer Model Context Protocol-servere.

```bash
opennovel mcp [command]
```

---

#### add

Legg til en MCP-server til konfigurasjonen.

```bash
opennovel mcp add
```

Denne kommandoen vil veilede deg gjennom å legge til enten en lokal eller ekstern MCP-server.

---

#### list

Liste alle konfigurerte MCP-servere og deres tilkoblingsstatus.

```bash
opennovel mcp list
```

Eller bruk den korte versjonen.

```bash
opennovel mcp ls
```

---

#### auth

Autentiser med en OAuth-aktivert MCP-server.

```bash
opennovel mcp auth [name]
```

Hvis du ikke oppgir et servernavn, blir du bedt om å velge blant tilgjengelige OAuth-kompatible servere.

Du kan også liste opp OAuth-kompatible servere og deres autentiseringsstatus.

```bash
opennovel mcp auth list
```

Eller bruk den korte versjonen.

```bash
opennovel mcp auth ls
```

---

#### logout

Fjern OAuth-påloggingsinformasjon for en MCP-server.

```bash
opennovel mcp logout [name]
```

---

#### debug

Feilsøk OAuth-tilkoblingsproblemer for en MCP-server.

```bash
opennovel mcp debug <name>
```

---

### models

List opp alle tilgjengelige modeller fra konfigurerte leverandører.

```bash
opennovel models [provider]
```

Denne kommandoen viser alle modeller som er tilgjengelige på tvers av dine konfigurerte leverandører i formatet `provider/model`.

Dette er nyttig for å finne ut det eksakte modellnavnet som skal brukes i [din konfigurasjon](/docs/config/).

Du kan eventuelt sende en leverandør ID for å filtrere modeller etter den leverandøren.

```bash
opennovel models anthropic
```

#### Flagg

| Flagg                                   | Beskrivelse                                                         |
| --------------------------------------- | ------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Oppdater modellbufferen fra models.dev                              |
| <nobr><code>{"--verbose"}</code></nobr> | Bruk mer detaljert modellutdata (inkluderer metadata som kostnader) |

Bruk `--refresh`-flagget for å oppdatere den bufrede modelllisten. Dette er nyttig når nye modeller er lagt til en leverandør og du vil se dem i OpenNovel.

```bash
opennovel models --refresh
```

---

### run

Kjør OpenNovel i ikke-interaktiv modus ved å sende en melding direkte.

```bash
opennovel run [message..]
```

Dette er nyttig for skripting, automatisering eller når du vil ha et raskt svar uten å starte hele TUI. For eksempel.

```bash "opennovel run"
opennovel run Explain the use of context in Go
```

Du kan også koble til en kjørende `opennovel serve`-forekomst for å unngå MCP-serveres kaldstarttid ved hver kjøring:

```bash
# Start a headless server in one terminal
opennovel serve

# In another terminal, run commands that attach to it
opennovel run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                                                          |
| ---------------------------------------- | ---- | ---------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |      | Kommandoen for å kjøre, bruk melding for args                                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                                               |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`)                             |
| <nobr><code>{"--share"}</code></nobr>    |      | Del økten                                                                                            |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | Modell å bruke i form av leverandør/modell                                                           |
| <nobr><code>{"--agent"}</code></nobr>    |      | Agent å bruke                                                                                        |
| <nobr><code>{"--file"}</code></nobr>     | `-f` | Fil(er) som skal legges ved meldingen                                                                |
| <nobr><code>{"--format"}</code></nobr>   |      | Format: standard (formatert) eller json (rå JSON hendelser)                                          |
| <nobr><code>{"--title"}</code></nobr>    |      | Tittel for økten (bruker avkortet ledetekst hvis ingen verdi er oppgitt)                             |
| <nobr><code>{"--attach"}</code></nobr>   |      | Koble til en kjørende OpenNovel-server (f.eks. http://localhost:4096)                                |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Passord for grunnleggende autentisering (standard: `OPENNOVEL_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Brukernavn for grunnleggende autentisering (standard: `OPENNOVEL_SERVER_USERNAME` eller `opennovel`) |
| <nobr><code>{"--dir"}</code></nobr>      |      | Katalog å kjøre i, eller sti på fjernserveren ved tilkobling                                         |
| <nobr><code>{"--variant"}</code></nobr>  |      | Modellvariant (leverandørspesifikk resonneringsinnsats)                                              |
| <nobr><code>{"--thinking"}</code></nobr> |      | Vis tenkeblokker                                                                                     |
| <nobr><code>{"--port"}</code></nobr>     |      | Port for den lokale serveren (standard til tilfeldig port)                                           |

---

### serve

Start en headless OpenNovel-server for API-tilgang. Sjekk ut [server-dokumentene](/docs/server) for hele HTTP-grensesnittet.

```bash
opennovel serve
```

Dette starter en HTTP-server som gir API tilgang til OpenNovel-funksjonalitet uten TUI-grensesnittet. Sett `OPENNOVEL_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `opennovel`).

#### Flagg

| Flagg                                    | Beskrivelse                                        |
| ---------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på                               |
| <nobr><code>{"--mdns"}</code></nobr>     | Aktiver mDNS-oppdagelse                            |
| <nobr><code>{"--cors"}</code></nobr>     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### session

Administrer OpenNovel-økter.

```bash
opennovel session [command]
```

---

#### list

List opp alle OpenNovel-økter.

```bash
opennovel session list
```

##### Flagg

| Flagg                                     | Kort | Beskrivelse                              |
| ----------------------------------------- | ---- | ---------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | Begrens til N siste økter                |
| <nobr><code>{"--format"}</code></nobr>    |      | Utdataformat: tabell eller json (tabell) |

---

### stats

Vis tokenbruk og kostnadsstatistikk for OpenNovel-øktene dine.

```bash
opennovel stats
```

#### Flagg

| Flagg                                   | Beskrivelse                                                                      |
| --------------------------------------- | -------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Vis statistikk for de siste N dagene (hele tiden)                                |
| <nobr><code>{"--tools"}</code></nobr>   | Antall verktøy som skal vises (alle)                                             |
| <nobr><code>{"--models"}</code></nobr>  | Vis oversikt over modellbruk (skjult som standard). Gi et tall for å vise topp N |
| <nobr><code>{"--project"}</code></nobr> | Filtrer etter prosjekt (alle prosjekter, tom streng: gjeldende prosjekt)         |

---

### export

Eksporter øktdata som JSON.

```bash
opennovel export [sessionID]
```

Hvis du ikke gir en økt ID, blir du bedt om å velge fra tilgjengelige økter.

---

### import

Importer øktdata fra en JSON-fil eller en OpenNovel share-URL.

```bash
opennovel import <file>
```

Du kan importere fra en lokal fil eller en OpenNovel share-URL.

```bash
opennovel import session.json
opennovel import https://opncd.ai/s/abc123
```

---

### web

Start en headless OpenNovel-server med et webgrensesnitt.

```bash
opennovel web
```

Dette starter en HTTP-server og åpner en nettleser for å få tilgang til OpenNovel via et nettgrensesnitt. Sett `OPENNOVEL_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `opennovel`).

#### Flagg

| Flagg                                    | Beskrivelse                                        |
| ---------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på                               |
| <nobr><code>{"--mdns"}</code></nobr>     | Aktiver mDNS-oppdagelse                            |
| <nobr><code>{"--cors"}</code></nobr>     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### acp

Start en ACP (Agent Client Protocol) server.

```bash
opennovel acp
```

Denne kommandoen starter en ACP-server som kommuniserer via stdin/stdout ved å bruke nd-JSON.

#### Flagg

| Flagg                                    | Beskrivelse          |
| ---------------------------------------- | -------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Arbeidskatalog       |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på      |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på |

---

### uninstall

Avinstaller OpenNovel og fjern alle relaterte filer.

```bash
opennovel uninstall
```

#### Flagg

| Flagg                                       | Kort | Beskrivelse                                   |
| ------------------------------------------- | ---- | --------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Behold konfigurasjonsfiler                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d` | Behold øktdata og øyeblikksbilder             |
| <nobr><code>{"--dry-run"}</code></nobr>     |      | Vis hva som ville blitt fjernet uten å fjerne |
| <nobr><code>{"--force"}</code></nobr>       | `-f` | Hopp over bekreftelsesforespørsler            |

---

### upgrade

Oppdaterer OpenNovel til siste versjon eller en spesifikk versjon.

```bash
opennovel upgrade [target]
```

For å oppgradere til siste versjon.

```bash
opennovel upgrade
```

For å oppgradere til en bestemt versjon.

```bash
opennovel upgrade v0.1.48
```

#### Flagg

| Flagg                                  | Kort | Beskrivelse                                                    |
| -------------------------------------- | ---- | -------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Installasjonsmetoden som ble brukt: curl, npm, pnpm, bun, brew |

---

## Globale flagg

OpenNovel CLI bruker følgende globale flagg.

| Flagg                                      | Kort | Beskrivelse                         |
| ------------------------------------------ | ---- | ----------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h` | Vis hjelp                           |
| <nobr><code>{"--version"}</code></nobr>    | `-v` | Skriv ut versjonsnummer             |
| <nobr><code>{"--print-logs"}</code></nobr> |      | Skriv ut logger til stderr          |
| <nobr><code>{"--log-level"}</code></nobr>  |      | Loggnivå (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

OpenNovel kan konfigureres ved hjelp av miljøvariabler.

| Variabel                               | Type   | Beskrivelse                                                            |
| -------------------------------------- | ------ | ---------------------------------------------------------------------- |
| `OPENNOVEL_AUTO_SHARE`                 | boolsk | Del økter automatisk                                                   |
| `OPENNOVEL_GIT_BASH_PATH`              | streng | Bane til Git Bash-kjørbar på Windows                                   |
| `OPENNOVEL_CONFIG`                     | streng | Bane til konfigurasjonsfil                                             |
| `OPENNOVEL_TUI_CONFIG`                 | streng | Bane til TUI-konfigurasjonsfil                                         |
| `OPENNOVEL_CONFIG_DIR`                 | streng | Bane til konfigurasjonskatalog                                         |
| `OPENNOVEL_CONFIG_CONTENT`             | streng | Innebygd json-konfigurasjonsinnhold                                    |
| `OPENNOVEL_DISABLE_AUTOUPDATE`         | boolsk | Deaktiver automatiske oppdateringskontroller                           |
| `OPENNOVEL_DISABLE_PRUNE`              | boolsk | Deaktiver beskjæring av gamle data                                     |
| `OPENNOVEL_DISABLE_TERMINAL_TITLE`     | boolsk | Deaktiver automatiske terminaltitteloppdateringer                      |
| `OPENNOVEL_PERMISSION`                 | streng | Innebygd json-tillatelseskonfigurasjon                                 |
| `OPENNOVEL_DISABLE_DEFAULT_PLUGINS`    | boolsk | Deaktiver standard plugins                                             |
| `OPENNOVEL_DISABLE_LSP_DOWNLOAD`       | boolsk | Deaktiver automatiske LSP servernedlastinger                           |
| `OPENNOVEL_ENABLE_EXPERIMENTAL_MODELS` | boolsk | Aktiver eksperimentelle modeller                                       |
| `OPENNOVEL_DISABLE_AUTOCOMPACT`        | boolsk | Deaktiver automatisk kontekstkomprimering                              |
| `OPENNOVEL_DISABLE_CLAUDE_CODE`        | boolsk | Deaktiver lesing fra `.claude` (spørsmål + ferdigheter)                |
| `OPENNOVEL_DISABLE_CLAUDE_CODE_PROMPT` | boolsk | Deaktiver lesing `~/.claude/CLAUDE.md`                                 |
| `OPENNOVEL_DISABLE_CLAUDE_CODE_SKILLS` | boolsk | Deaktiver innlasting av `.claude/skills`                               |
| `OPENNOVEL_DISABLE_MODELS_FETCH`       | boolsk | Deaktiver henting av modeller fra eksterne kilder                      |
| `OPENNOVEL_FAKE_VCS`                   | streng | Falsk VCS-leverandør for testformål                                    |
| `OPENNOVEL_CLIENT`                     | streng | Klientidentifikator (standard til `cli`)                               |
| `OPENNOVEL_ENABLE_EXA`                 | boolsk | Aktiver Exa-nettsøkeverktøy                                            |
| `OPENNOVEL_SERVER_PASSWORD`            | streng | Aktiver grunnleggende autentisering for `serve`/`web`                  |
| `OPENNOVEL_SERVER_USERNAME`            | streng | Overstyr grunnleggende autentiseringsbrukernavn (standard `opennovel`) |
| `OPENNOVEL_MODELS_URL`                 | streng | Egendefinert URL for henting av modellkonfigurasjon                    |

---

### Eksperimentelt

Disse miljøvariablene muliggjør eksperimentelle funksjoner som kan endres eller fjernes.

| Variabel                                         | Type   | Beskrivelse                                           |
| ------------------------------------------------ | ------ | ----------------------------------------------------- |
| `OPENNOVEL_EXPERIMENTAL`                         | boolsk | Aktiver eksperimentelle funksjoner under samleflagget |
| `OPENNOVEL_EXPERIMENTAL_ICON_DISCOVERY`          | boolsk | Aktiver ikonoppdagelse                                |
| `OPENNOVEL_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolsk | Deaktiver kopi ved valg i TUI                         |
| `OPENNOVEL_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | tall   | Standard tidsavbrudd for bash-kommandoer i ms         |
| `OPENNOVEL_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | tall   | Maks Output Tokens for LLM-svar                       |
| `OPENNOVEL_EXPERIMENTAL_FILEWATCHER`             | boolsk | Aktiver filovervåker for hele dir                     |
| `OPENNOVEL_EXPERIMENTAL_OXFMT`                   | boolsk | Aktiver oxfmt formatter                               |
| `OPENNOVEL_EXPERIMENTAL_LSP_TOOL`                | boolsk | Aktiver eksperimentelt LSP-verktøy                    |
| `OPENNOVEL_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolsk | Deaktiver filovervåking                               |
| `OPENNOVEL_EXPERIMENTAL_EXA`                     | boolsk | Aktiver eksperimentelle Exa-funksjoner                |
| `OPENNOVEL_EXPERIMENTAL_LSP_TY`                  | boolsk | Aktiver TY LSP for python-filer                       |
| `OPENNOVEL_EXPERIMENTAL_PLAN_MODE`               | boolsk | Aktiver planmodus                                     |
| `OPENNOVEL_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolsk | Aktiver bakgrunnsoppgaver for subagenter              |
| `OPENNOVEL_EXPERIMENTAL_EVENT_SYSTEM`            | boolsk | Aktiver eksperimentelt hendelsessystem                |
| `OPENNOVEL_EXPERIMENTAL_NATIVE_LLM`              | boolsk | Aktiver innebygd LLM-forespørselsvei                  |
| `OPENNOVEL_EXPERIMENTAL_PARALLEL`                | boolsk | Aktiver parallell kjøring av websøk                   |
| `OPENNOVEL_EXPERIMENTAL_SCOUT`                   | boolsk | Aktiver Scout-subagent                                |
| `OPENNOVEL_EXPERIMENTAL_WORKSPACES`              | boolsk | Aktiver arbeidsområde-støtte                          |
